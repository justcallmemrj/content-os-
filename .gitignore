# Secrets — never in the repo (D-069, HK5, pre-commit scan)
.env
.env.*
*.pem
*.key
*_rsa
*_rsa.*
credentials*
secrets/

# Local-only instructions
CLAUDE.local.md

# Live workflow DB + snapshots: DDL is versioned (state/ddl.sql); binary is not
# (DEC-BUILD-004, ratified 2026-07-10). Backups via Phase 7 §8 legs.
state/*.sqlite
state/checkpoints/

# Python
__pycache__/
*.pyc
.venv/

# OS noise
.DS_Store
Thumbs.db

# Engine workspace node_modules (Phase 7; the pin lives in package.json, DEC-BUILD-008)
video/*/node_modules/
