# Content OS — Design Bundle (v1.0.1)

Unzip into the ROOT of the `content-os` repo so `docs/` sits at repo root.
docs/architecture/ = the seven ratified specifications (READ-ONLY to the build, D-078).
docs/build/ = Phase 8 package + master prompt + slice prompt.
Verify the specs after copying: `sha256sum -c CHECKSUMS.sha256` (from repo root,
paths in the file are relative to docs/architecture/ — cd there first, or adjust).
Authority order: live instruction from Wes → decision log D-001–D-080 → these specs
→ current official vendor docs, re-verified.
