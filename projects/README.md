# projects/ — the four brand workspaces (L1)

`benowitz-wealth` (BEN) · `ducat-private-wealth` (DUC) · `trading-research`
(TRD) · `founder-brand` (FDR). Activation order: BEN (slice) → DUC → TRD → FDR
(D-007, package §9). `_shared/` holds the RIA compliance envelope, included by
explicit reference only (Phase 3 §3.5) — shared truth, zero implicit access.

Exactly one project loads per run (Phase 3 §3). Profile files, brand voice,
compliance, disclosures, approved-facts, sources, decisions, lessons are
PROTECTED (Phase 2 §4.4). All content here arrives via step 3 seeding as
proposals (D-079) — directories are scaffold-empty until Wes's first H6 merge.
